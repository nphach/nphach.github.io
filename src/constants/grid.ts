export const GRID_BLOCK_SIZE_RATIO = 0.05;
export const GRID_COLOR = "38, 48, 34";
export const GRID_COLUMN_COUNT = 20;

export const GRID_COLUMNS = Array.from(
  { length: GRID_COLUMN_COUNT },
  (_, index) => index,
);
